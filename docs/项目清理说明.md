# 项目清理说明

## 📋 清理时间
2025-12-29

## 🗑️ 已删除的文件

### 1. 未使用的工具文件

#### `utils/playwright_helper.py`
- **原因**：未被任何文件导入使用
- **说明**：这是之前用于解决事件循环问题的遗留代码，现在已通过修改 pytest fixture scope 解决了问题，不再需要此文件

#### `utils/recording_helper.py`
- **原因**：未被任何文件导入使用
- **说明**：功能已被以下文件替代：
  - `utils/recording_auto_login.py` - 自动登录功能
  - `tools/convert_recording.py` - 代码转换功能（在 WebUI 中使用）

### 2. 无用的测试文件

#### `test_cases/exam/test_exam_basic.py`
- **原因**：只有占位代码和 TODO 注释，没有实际测试逻辑
- **说明**：如果需要考试测评的测试用例，应该创建实际的测试文件，而不是保留这个占位文件

### 3. 未使用的核心模块（已恢复）

#### `core/db_client.py`
- **原因**：只在 README.md 中作为示例提到，但实际代码中没有使用
- **说明**：已恢复，因为后续需要使用 MySQL 数据库功能
- **恢复时间**：2025-12-29

## ✅ 保留的文件（说明）

### 模板文件
- **`test_cases/teaching/test_template.py`** - 测试用例模板
  - 有 `@pytest.mark.skip` 标记，不会被 pytest 执行
  - 作为新测试用例的参考模板，应该保留

### 工具文件
- **`tools/convert_recording.py`** - 代码转换工具
  - 在 `web_ui/main.py` 中被导入使用
  - 用于将录制的同步代码转换为异步代码

### 文档文件
所有文档文件都有各自的用途，已保留：
- `docs/录制指南.md` - 简化版录制指南（实习生版）
- `docs/录制工具使用说明.md` - 详细的录制工具说明
- `docs/代码转换工具使用说明.md` - 代码转换工具说明
- `docs/测试报告说明.md` - 测试报告功能说明
- `docs/网络访问说明.md` - 网络地址说明
- `docs/录制时如何生成断言.md` - 断言生成说明
- `docs/页面对象说明.md` - 页面对象模式说明
- `docs/Web界面使用说明.md` - Web界面使用说明
- `docs/temp目录说明.md` - 临时目录说明

## 📝 更新的文件

### `README.md`
- 删除了对已删除文件的引用：
  - `core/db_client.py`
  - `utils/recording_helper.py`
  - `pages/teaching_app.py`、`pages/exam_app.py`、`pages/exercise_app.py`（这些文件不存在）
- 更新了工具文件列表，反映实际使用的文件

## 🎯 清理结果

- **删除文件数**：4 个
- **更新文件数**：1 个（README.md）
- **项目结构更清晰**：移除了未使用的代码，减少了混淆

## ⚠️ 注意事项

1. **`core/db_client.py` 已恢复**：因为后续需要使用 MySQL 数据库功能
2. **如果将来需要考试测评测试用例**，应该创建实际的测试文件，而不是使用占位文件
3. **所有删除的文件都可以从 Git 历史中恢复**（如果已提交到版本控制）

## 🔄 后续更新

### 2025-12-29
- **恢复 `core/db_client.py`**：因为后续需要使用 MySQL 数据库功能
- 文件包含完整的数据库操作功能：
  - 支持直接连接和SSH隧道连接
  - 提供查询、更新、批量更新方法
  - 支持上下文管理器，自动管理连接

## 📊 清理前后对比

### 清理前
- 包含未使用的工具文件
- 包含占位测试文件
- 包含未使用的数据库客户端
- README 中引用了不存在的文件

### 清理后
- 只保留实际使用的文件
- 项目结构更清晰
- README 与实际代码一致
- 减少了维护负担

